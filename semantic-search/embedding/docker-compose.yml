services:
  chromadb-sentence:
    image: chromadb/chroma:1.1.1
    container_name: chromadb-sentence
    ports:
      - "8000:8000"
    volumes:
      - ../../volumes/chroma_sentence:/data
    environment:
      - IS_PERSISTENT=TRUE
      - PERSIST_DIRECTORY=/data
      - ANONYMIZED_TELEMETRY=FALSE

  chromadb-error-words:
    image: chromadb/chroma:1.1.1
    container_name: chromadb-error-words
    ports:
      - "8001:8000"
    volumes:
      - ../../volumes/chroma_error_words:/data
    environment:
      - IS_PERSISTENT=TRUE
      - PERSIST_DIRECTORY=/data
      - ANONYMIZED_TELEMETRY=FALSE